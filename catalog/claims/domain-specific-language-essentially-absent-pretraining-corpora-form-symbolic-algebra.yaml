id: domain-specific-language-essentially-absent-pretraining-corpora-form-symbolic-algebra
ref: CLM-0351
capability: code-generation
technique: mine-training-data-through-language-s-own-interpreter
statement: >-
  For a domain-specific language essentially absent from pretraining corpora
  (FORM symbolic algebra), QLoRA fine-tuning an 8B model on a few thousand
  examples validated by executing them through the language's own
  interpreter beats frontier open-weight models of up to 756B parameters
  given syntax documentation in context, on execution rate and strict output
  match for the larger benchmarks, while general reasoning scores drop under
  3 points.
kind: mechanism
backing_strength: single-paper
observed_on:
  era: "Zero-resource DSL with a deterministic execution oracle; single-attempt, temperature-0 generation; frontier baselines are open-weight only and get 6,000 characters of docs; benchma"
sources:
  - source: arxiv-2609-23367
    stance: supports
    note: >-
      Validated by: 840 single-attempt tasks across four benchmarks: 97.7%
      exact output match on 664 deterministic tasks vs 75.5% for GLM-5.2 and
      62.3% for DeepSeek-V4 Flash (non-overlapping 95% CIs); 83.0% execution
      and 18.0% strict match on Instruct-100 vs 65.0%/5.6% best frontier
      (McNemar p<=0.007); no lead on the 41- and 22-task benchmarks
      (p>=0.375); MMLU -2.6pp, GSM8K -1.5pp, HumanEval -0.6pp.. Measured,
      single author, single base model, author-built benchmarks. Verified by
      execution against the FORM binary with difference-to-zero equality,
      which is strong. [truncated]
contested: false
status: pending-review
last_checked_at: 2026-09-25
notes: >-
  Drafted from the paper by a model and filed unreviewed. Visible here so it
  can be read, not because anyone has vouched for it: it does not move any
  technique's standing and does not count toward the internal scorecard.
  Drafted confidence: medium. Falsifier as drafted: A frontier model with
  the same documentation matching or beating the fine-tuned 8B specialist on
  the 664-task deterministic and Instruct-100 strict metrics, or the
  specialist's lead disappearing on a benchmark built by an independent
  party.
submitted_by: agent:claude-opus-5@Russ-Miller