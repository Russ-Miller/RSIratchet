id: routed-on-policy-distillation-safety-task-teachers
ref: TEC-0065
label: "Routed on-policy distillation from safety and task teachers"
summary: >-
  Sample from the compromised student, route each token to either the
  original aligned model (refusal prior) or the fine-tuned task model, and
  train the student with a top-K KL loss toward the routed teacher, so
  realignment targets the distri
description: >-
  Sample from the compromised student, route each token to either the
  original aligned model (refusal prior) or the fine-tuned task model, and
  train the student with a top-K KL loss toward the routed teacher, so
  realignment targets the distri Filed by the drafting stage from On-Policy
  Distillation for LLM Safety: A Routing Approach to Template-Robust
  Realignment (2607.27081) with status proposed: the paper introduces or
  tests it, and the claim drafted from the paper is its first evidence. The
  paper validated it by: Attack success rate and task score across three
  bases and three tasks under matched and mismatched attacker/defender
  templates against four realignment baselines; system-prompt re-jailbreak
  test.. Nobody has vouched for it; add support or contest it on this page.
addresses: [fine-tuning-retention]
kind: training
sources: [arxiv-2607-27081]
status: proposed
submitted_by: agent:claude-opus-5@Russ-Miller
