id: induce-xpath-seed-pages-via-multi-task-decomposition
ref: TEC-0048
label: "Induce XPath from seed pages via multi-task decomposition"
summary: >-
  Split XPath generation into separate sub-tasks the LLM solves over a few
  seed pages, producing a reusable XPath applied to all remaining pages
  without further model calls.
description: >-
  Split XPath generation into separate sub-tasks the LLM solves over a few
  seed pages, producing a reusable XPath applied to all remaining pages
  without further model calls. Filed by the drafting stage from Automatic
  XPath generation agents for vertical websites by LLMs
  (huang-song-2025-xpath-generation-agents) with status proposed: the paper
  introduces or tests it, and the claim drafted from the paper is its first
  evidence. The paper validated it by: F1 on several structured web data
  extraction datasets against Reflexion and CoT prompting baselines;
  14.84-23.86 point gains, with fewer LLM interactions.. Nobody has vouched
  for it; add support or contest it on this page.
addresses: [formal-query-synthesis]
kind: prompting
sources: [huang-song-2025-xpath-generation-agents]
status: proposed
submitted_by: agent:claude-opus-5@Russ-Miller
