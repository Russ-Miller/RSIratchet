id: conformal-abstention-self-consistency
ref: TEC-0054
label: "Conformal abstention from self-consistency"
summary: >-
  Draw k responses, have the model count how many match each candidate, use
  that match score as confidence, and set the abstention threshold on a
  labelled calibration set with conformal prediction so the expected error
  among answered question
description: >-
  Draw k responses, have the model count how many match each candidate, use
  that match score as confidence, and set the abstention threshold on a
  labelled calibration set with conformal prediction so the expected error
  among answered question Filed by the drafting stage from Mitigating LLM
  Hallucinations via Conformal Abstention (2405.01563) with status proposed:
  the paper introduces or tests it, and the claim drafted from the paper is
  its first evidence. The paper validated it by: Hallucination rate versus
  abstention rate curves on TriviaQA and Temporal Sequences against
  log-probability and prior self-consistency baselines; error bound verified
  empirically against manual labels on a 100-item calibration set.. Nobody
  has vouched for it; add support or contest it on this page.
addresses: [abstention-and-self-knowledge]
kind: decoding
sources: [arxiv-2405-01563]
status: proposed
submitted_by: agent:claude-opus-5@Russ-Miller
