id: repository-code-graph-navigation-plug-in
ref: TEC-0059
label: "Repository code graph as a navigation plug-in"
summary: >-
  Build a graph whose nodes are definition and reference lines and whose
  edges are contain and invoke relations across the whole repository, then
  answer the agent's or pipeline's lookups with the sub-graph around a
  queried symbol so it sees c
description: >-
  Build a graph whose nodes are definition and reference lines and whose
  edges are contain and invoke relations across the whole repository, then
  answer the agent's or pipeline's lookups with the sub-graph around a
  queried symbol so it sees c Filed by the drafting stage from RepoGraph:
  Enhancing AI Software Engineering with Repository-level Code Graph
  (2410.14684) with status proposed: the paper introduces or tests it, and
  the claim drafted from the paper is its first evidence. The paper
  validated it by: Resolve rate and cost on SWE-bench Lite for each of four
  frameworks with and without the plug-in; ablation over ego-graph depth;
  secondary test on CrossCodeEval code completion.. Nobody has vouched for
  it; add support or contest it on this page.
addresses: [repository-navigation]
kind: tooling
sources: [arxiv-2410-14684]
status: proposed
submitted_by: agent:claude-opus-5@Russ-Miller
