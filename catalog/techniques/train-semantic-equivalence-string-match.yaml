id: train-semantic-equivalence-string-match
ref: TEC-0051
label: "Train on semantic equivalence, not string match"
summary: >-
  Replaces the MLE objective with a reward that maximizes expected semantic
  correctness of the generated regex, measured by a DFA-equivalence oracle
  plus random and distinguishing test cases.
description: >-
  Replaces the MLE objective with a reward that maximizes expected semantic
  correctness of the generated regex, measured by a DFA-equivalence oracle
  plus random and distinguishing test cases. Filed by the drafting stage
  from SemRegex: A Semantics-Based Approach for Generating Regular
  Expressions from Natural Language Specifications (zhong-2018-semregex)
  with status proposed: the paper introduces or tests it, and the claim
  drafted from the paper is its first evidence. The paper validated it by:
  Semantic accuracy against prior state-of-the-art syntax-based models on
  three public NL-to-regex datasets; the paper reports superiority but the
  excerpt gives no numbers.. Nobody has vouched for it; add support or
  contest it on this page.
addresses: [formal-query-synthesis]
kind: training
sources: [zhong-2018-semregex]
status: proposed
submitted_by: agent:claude-opus-5@Russ-Miller
