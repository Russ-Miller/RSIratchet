id: priority-scheduled-search-distance-aware-context-pruning
ref: TEC-0057
label: "Priority-scheduled search with distance-aware context prunin"
summary: >-
  Keep a scored queue of code entities to inspect, let the model decompose
  each search into smaller lookups it scores for relevance, and drop context
  whose graph distance from already-relevant anchors is large, so the agent
  spends its budget
description: >-
  Keep a scored queue of code entities to inspect, let the model decompose
  each search into smaller lookups it scores for relevance, and drop context
  whose graph distance from already-relevant anchors is large, so the agent
  spends its budget Filed by the drafting stage from OrcaLoca: An LLM Agent
  Framework for Software Issue Localization (2502.00350) with status
  proposed: the paper introduces or tests it, and the claim drafted from the
  paper is its first evidence. The paper validated it by: Function, file and
  class match rates on SWE-bench Lite against prior open-source localizers,
  with ablations removing scheduling, decomposition and pruning; downstream
  resolved-rate change when its output replaces the baseline's
  localization.. Nobody has vouched for it; add support or contest it on
  this page.
addresses: [repository-navigation]
kind: process
sources: [arxiv-2502-00350]
status: proposed
submitted_by: agent:claude-opus-5@Russ-Miller
