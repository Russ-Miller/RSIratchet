id: refusal-aware-instruction-tuning
ref: TEC-0058
label: "Refusal-aware instruction tuning"
summary: >-
  Run the base model on the tuning set, label each example as known or
  unknown by whether it answered correctly, keep answers for the known set
  and append an uncertainty expression to the unknown set, then fine-tune on
  the union so the model
description: >-
  Run the base model on the tuning set, label each example as known or
  unknown by whether it answered correctly, keep answers for the known set
  and append an uncertainty expression to the unknown set, then fine-tune on
  the union so the model Filed by the drafting stage from R-Tuning:
  Instructing Large Language Models to Say `I Don't Know' (2311.09677) with
  status proposed: the paper introduces or tests it, and the claim drafted
  from the paper is its first evidence. The paper validated it by: AP scores
  in single-task and multi-task settings against vanilla instruction tuning
  on the same data, plus out-of-domain AP on HaluEval-QA and a comparison
  against uncertainty-based filtering at test time; 3B-13B open models
  only.. Nobody has vouched for it; add support or contest it on this page.
addresses: [abstention-and-self-knowledge]
kind: training
sources: [arxiv-2311-09677]
status: proposed
submitted_by: agent:claude-opus-5@Russ-Miller
