generated_at: 2026-09-19
from_raw_proposals: 3
recommended:
  - id: formal-query-synthesis
    label: Turning intent or examples into a formal query or pattern
    scope_note: Covers producing executable queries, regexes, selectors, or other
      pattern-language artifacts whose semantics must match a user's stated
      intent or a few given examples; excludes writing ordinary application
      code from an explicit spec, which code-generation already covers.
    paper_count: 2
    merged_from:
      - nl-to-formal-query
      - program-synthesis-from-examples
    note: Semantic fidelity to intent in constrained formal languages is a
      distinct failure mode from general code-generation, and both proposals
      point at the same skill, so it is worth one entry despite thin evidence.
set_aside:
  - id: structured-document-extraction
    note: A single-paper task domain rather than a capability, and its
      generalizable part (inducing selectors) already falls under
      formal-query-synthesis while the rest is covered by
      retrieval-utilization and visual-grounding.
